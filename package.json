{
  "name": "papavoice-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -R papavoice public/papavoice && cp index.html public/index.html",
    "check": "node --check papavoice/app.js && node --check api/status.js && node --check api/create-voice.js && node --check api/generate-audio.js",
    "test": "node --test",
    "dev": "vercel dev"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
